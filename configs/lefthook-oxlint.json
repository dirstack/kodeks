{
  "$schema": "https://json.schemastore.org/lefthook.json",
  "pre-commit": {
    "parallel": true,
    "commands": {
      "lint:oxlint": {
        "glob": "*.{ts,tsx,js,jsx}",
        "run": "bunx oxlint --fix {staged_files}",
        "stage_fixed": true
      }
    }
  }
}
